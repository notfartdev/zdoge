{
  "name": "dogenado-contracts",
  "version": "1.0.0",
  "description": "Dogenado Privacy Pool Smart Contracts for DogeOS",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network dogeosTestnet",
    "verify": "hardhat verify --network dogeosTestnet",
    "clean": "hardhat clean",
    "node": "hardhat node"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "circomlibjs": "^0.1.7",
    "dotenv": "^16.3.1",
    "ffjavascript": "^0.3.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0"
  }
}

